# Shared agent skills compatibility pointer

The canonical skill content for `rise-up` lives in the top-level `skills/` tree.

Use `skills/README.md` for the skill contract and catalog rules.

Keep `.claude/skills/` as a thin compatibility path only.